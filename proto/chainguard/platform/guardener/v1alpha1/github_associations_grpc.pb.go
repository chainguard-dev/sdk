// Copyright 2026 Chainguard, Inc.
// SPDX-License-Identifier: Apache-2.0

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.1
// source: chainguard/platform/guardener/v1alpha1/github_associations.proto

package v1alpha1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	GitHubAssociations_BeginGitHubOAuth_FullMethodName         = "/chainguard.platform.guardener.v1alpha1.GitHubAssociations/BeginGitHubOAuth"
	GitHubAssociations_LinkGitHubOrganization_FullMethodName   = "/chainguard.platform.guardener.v1alpha1.GitHubAssociations/LinkGitHubOrganization"
	GitHubAssociations_UnlinkGitHubOrganization_FullMethodName = "/chainguard.platform.guardener.v1alpha1.GitHubAssociations/UnlinkGitHubOrganization"
)

// GitHubAssociationsClient is the client API for GitHubAssociations service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// GitHubAssociations is the guardener self-service surface for linking a GitHub
// organization to a Chainguard group, and unlinking it again.
//
// Both mutating RPCs take a GitHub OAuth authorization code obtained by the
// client's localhost flow; the server exchanges it for a user-to-server token
// and verifies, one time, that the caller is an owner of the organization (and
// that the guardener GitHub App is installed on it). The user token is never
// persisted.
type GitHubAssociationsClient interface {
	// BeginGitHubOAuth starts a GitHub OAuth localhost flow. It returns the
	// guardener GitHub App's OAuth client ID and a short-lived signed `state` the
	// client threads through the authorize round trip and hands back on the
	// mutating RPC. (When linking fails because the App isn't installed, the
	// install URL is surfaced in the LinkGitHubOrganization error, not here.)
	//
	// The caller must be authenticated (a valid Chainguard token); no capability
	// is required. The interceptor auth is disabled — the handler itself requires
	// a token — so the `state` it mints attests that a logged-in Chainguard user
	// initiated the flow. The server verifies that state before performing any
	// GitHub token exchange, which is what keeps the (otherwise Chainguard-
	// unauthenticated) GitHub-ownership unlink path behind a login.
	BeginGitHubOAuth(ctx context.Context, in *BeginGitHubOAuthRequest, opts ...grpc.CallOption) (*BeginGitHubOAuthResponse, error)
	// LinkGitHubOrganization links a GitHub organization to a Chainguard group.
	// The caller must hold CAP_GUARDENER_ASSOCIATION_MANAGE on the group (owner
	// on the Chainguard side) AND prove, via the GitHub OAuth code, that they are
	// an owner of the organization (owner on the GitHub side).
	LinkGitHubOrganization(ctx context.Context, in *LinkGitHubOrganizationRequest, opts ...grpc.CallOption) (*LinkGitHubOrganizationResponse, error)
	// UnlinkGitHubOrganization removes a GitHub organization's association.
	//
	// The caller proves authorization one of two ways (a oneof on the request):
	//   - chainguard: the caller's Chainguard token holds
	//     guardener.association.manage on the group that owns the org. This is the
	//     preferred path for someone who already administers the group.
	//   - github_oauth: a fresh GitHub OAuth code proving the caller is an owner
	//     of the org, plus the signed `state` from BeginGitHubOAuth. This lets a
	//     GitHub owner detach an org without group access — but still requires a
	//     Chainguard login (the state is minted only to authenticated callers).
	//
	// The method's interceptor auth is disabled because the credential check
	// differs per path; the handler enforces the appropriate check (group
	// capability, or signed state + GitHub ownership) for whichever is supplied.
	UnlinkGitHubOrganization(ctx context.Context, in *UnlinkGitHubOrganizationRequest, opts ...grpc.CallOption) (*UnlinkGitHubOrganizationResponse, error)
}

type gitHubAssociationsClient struct {
	cc grpc.ClientConnInterface
}

func NewGitHubAssociationsClient(cc grpc.ClientConnInterface) GitHubAssociationsClient {
	return &gitHubAssociationsClient{cc}
}

func (c *gitHubAssociationsClient) BeginGitHubOAuth(ctx context.Context, in *BeginGitHubOAuthRequest, opts ...grpc.CallOption) (*BeginGitHubOAuthResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BeginGitHubOAuthResponse)
	err := c.cc.Invoke(ctx, GitHubAssociations_BeginGitHubOAuth_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gitHubAssociationsClient) LinkGitHubOrganization(ctx context.Context, in *LinkGitHubOrganizationRequest, opts ...grpc.CallOption) (*LinkGitHubOrganizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(LinkGitHubOrganizationResponse)
	err := c.cc.Invoke(ctx, GitHubAssociations_LinkGitHubOrganization_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *gitHubAssociationsClient) UnlinkGitHubOrganization(ctx context.Context, in *UnlinkGitHubOrganizationRequest, opts ...grpc.CallOption) (*UnlinkGitHubOrganizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(UnlinkGitHubOrganizationResponse)
	err := c.cc.Invoke(ctx, GitHubAssociations_UnlinkGitHubOrganization_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// GitHubAssociationsServer is the server API for GitHubAssociations service.
// All implementations must embed UnimplementedGitHubAssociationsServer
// for forward compatibility.
//
// GitHubAssociations is the guardener self-service surface for linking a GitHub
// organization to a Chainguard group, and unlinking it again.
//
// Both mutating RPCs take a GitHub OAuth authorization code obtained by the
// client's localhost flow; the server exchanges it for a user-to-server token
// and verifies, one time, that the caller is an owner of the organization (and
// that the guardener GitHub App is installed on it). The user token is never
// persisted.
type GitHubAssociationsServer interface {
	// BeginGitHubOAuth starts a GitHub OAuth localhost flow. It returns the
	// guardener GitHub App's OAuth client ID and a short-lived signed `state` the
	// client threads through the authorize round trip and hands back on the
	// mutating RPC. (When linking fails because the App isn't installed, the
	// install URL is surfaced in the LinkGitHubOrganization error, not here.)
	//
	// The caller must be authenticated (a valid Chainguard token); no capability
	// is required. The interceptor auth is disabled — the handler itself requires
	// a token — so the `state` it mints attests that a logged-in Chainguard user
	// initiated the flow. The server verifies that state before performing any
	// GitHub token exchange, which is what keeps the (otherwise Chainguard-
	// unauthenticated) GitHub-ownership unlink path behind a login.
	BeginGitHubOAuth(context.Context, *BeginGitHubOAuthRequest) (*BeginGitHubOAuthResponse, error)
	// LinkGitHubOrganization links a GitHub organization to a Chainguard group.
	// The caller must hold CAP_GUARDENER_ASSOCIATION_MANAGE on the group (owner
	// on the Chainguard side) AND prove, via the GitHub OAuth code, that they are
	// an owner of the organization (owner on the GitHub side).
	LinkGitHubOrganization(context.Context, *LinkGitHubOrganizationRequest) (*LinkGitHubOrganizationResponse, error)
	// UnlinkGitHubOrganization removes a GitHub organization's association.
	//
	// The caller proves authorization one of two ways (a oneof on the request):
	//   - chainguard: the caller's Chainguard token holds
	//     guardener.association.manage on the group that owns the org. This is the
	//     preferred path for someone who already administers the group.
	//   - github_oauth: a fresh GitHub OAuth code proving the caller is an owner
	//     of the org, plus the signed `state` from BeginGitHubOAuth. This lets a
	//     GitHub owner detach an org without group access — but still requires a
	//     Chainguard login (the state is minted only to authenticated callers).
	//
	// The method's interceptor auth is disabled because the credential check
	// differs per path; the handler enforces the appropriate check (group
	// capability, or signed state + GitHub ownership) for whichever is supplied.
	UnlinkGitHubOrganization(context.Context, *UnlinkGitHubOrganizationRequest) (*UnlinkGitHubOrganizationResponse, error)
	mustEmbedUnimplementedGitHubAssociationsServer()
}

// UnimplementedGitHubAssociationsServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedGitHubAssociationsServer struct{}

func (UnimplementedGitHubAssociationsServer) BeginGitHubOAuth(context.Context, *BeginGitHubOAuthRequest) (*BeginGitHubOAuthResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method BeginGitHubOAuth not implemented")
}
func (UnimplementedGitHubAssociationsServer) LinkGitHubOrganization(context.Context, *LinkGitHubOrganizationRequest) (*LinkGitHubOrganizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method LinkGitHubOrganization not implemented")
}
func (UnimplementedGitHubAssociationsServer) UnlinkGitHubOrganization(context.Context, *UnlinkGitHubOrganizationRequest) (*UnlinkGitHubOrganizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method UnlinkGitHubOrganization not implemented")
}
func (UnimplementedGitHubAssociationsServer) mustEmbedUnimplementedGitHubAssociationsServer() {}
func (UnimplementedGitHubAssociationsServer) testEmbeddedByValue()                            {}

// UnsafeGitHubAssociationsServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to GitHubAssociationsServer will
// result in compilation errors.
type UnsafeGitHubAssociationsServer interface {
	mustEmbedUnimplementedGitHubAssociationsServer()
}

func RegisterGitHubAssociationsServer(s grpc.ServiceRegistrar, srv GitHubAssociationsServer) {
	// If the following call panics, it indicates UnimplementedGitHubAssociationsServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&GitHubAssociations_ServiceDesc, srv)
}

func _GitHubAssociations_BeginGitHubOAuth_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BeginGitHubOAuthRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitHubAssociationsServer).BeginGitHubOAuth(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitHubAssociations_BeginGitHubOAuth_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitHubAssociationsServer).BeginGitHubOAuth(ctx, req.(*BeginGitHubOAuthRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _GitHubAssociations_LinkGitHubOrganization_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(LinkGitHubOrganizationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitHubAssociationsServer).LinkGitHubOrganization(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitHubAssociations_LinkGitHubOrganization_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitHubAssociationsServer).LinkGitHubOrganization(ctx, req.(*LinkGitHubOrganizationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _GitHubAssociations_UnlinkGitHubOrganization_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(UnlinkGitHubOrganizationRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(GitHubAssociationsServer).UnlinkGitHubOrganization(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: GitHubAssociations_UnlinkGitHubOrganization_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(GitHubAssociationsServer).UnlinkGitHubOrganization(ctx, req.(*UnlinkGitHubOrganizationRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// GitHubAssociations_ServiceDesc is the grpc.ServiceDesc for GitHubAssociations service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var GitHubAssociations_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "chainguard.platform.guardener.v1alpha1.GitHubAssociations",
	HandlerType: (*GitHubAssociationsServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "BeginGitHubOAuth",
			Handler:    _GitHubAssociations_BeginGitHubOAuth_Handler,
		},
		{
			MethodName: "LinkGitHubOrganization",
			Handler:    _GitHubAssociations_LinkGitHubOrganization_Handler,
		},
		{
			MethodName: "UnlinkGitHubOrganization",
			Handler:    _GitHubAssociations_UnlinkGitHubOrganization_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "chainguard/platform/guardener/v1alpha1/github_associations.proto",
}
