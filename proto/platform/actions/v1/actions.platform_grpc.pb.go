// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.1
// source: actions.platform.proto

package v1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ActionsAuthorization_AuthorizeWithOIDC_FullMethodName = "/chainguard.platform.actions.ActionsAuthorization/AuthorizeWithOIDC"
	ActionsAuthorization_IssueChallenge_FullMethodName    = "/chainguard.platform.actions.ActionsAuthorization/IssueChallenge"
	ActionsAuthorization_VerifyChallenge_FullMethodName   = "/chainguard.platform.actions.ActionsAuthorization/VerifyChallenge"
	ActionsAuthorization_GetCACert_FullMethodName         = "/chainguard.platform.actions.ActionsAuthorization/GetCACert"
	ActionsAuthorization_GetTrustedKeys_FullMethodName    = "/chainguard.platform.actions.ActionsAuthorization/GetTrustedKeys"
)

// ActionsAuthorizationClient is the client API for ActionsAuthorization service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ActionsAuthorization issues authorization decisions for callers
// running inside GitHub Actions workflows.
//
// Two attestation mechanisms are provided:
//
//  1. AuthorizeWithOIDC verifies a GitHub Actions OIDC ID token
//     directly. This is the preferred path; trust derives from
//     GitHub's OIDC signing key.
//
//  2. IssueChallenge / VerifyChallenge implement an artifact-based
//     attestation, intended for workflows that *cannot* mint an OIDC
//     token at all. The most common case is a workflow run triggered
//     by a pull request from a fork: such runs do not get the
//     `id-token: write` permission elevated to a real token, so
//     AuthorizeWithOIDC is unavailable to them. The challenge flow
//     side-steps OIDC entirely and instead uses the run's own
//     artifact storage as the trust channel.
//
//     The server mints a short-lived X.509 leaf cert bound to the
//     run; the workflow uploads the cert as an artifact named with a
//     well-known prefix; verification fetches that artifact, validates
//     the cert against the trusted KMS public keys, and confirms its
//     URI SAN matches the requested run identity. Trust derives from
//     two facts: the cert is signed by our KMS key, and the
//     artifact's presence in the named run proves it was uploaded
//     from inside that run's runner (artifact upload requires
//     ACTIONS_RUNTIME_TOKEN, a per-runner credential GitHub never
//     exposes to workflow code). The mid-run logs API does not expose
//     new content until the run completes, so logs are not used.
//
// A successful AuthorizationResponse confirms the identity is real
// and that the caller is entitled to use this service. It is not a
// downstream permission grant — services that consume the returned
// `claims` (typically repository_owner_id and repository_visibility)
// apply their own authorization on top. Failure is signaled by a
// non-OK gRPC status (typically codes.PermissionDenied with no detail;
// see the README for the privacy rationale).
type ActionsAuthorizationClient interface {
	// AuthorizeWithOIDC verifies a GitHub Actions OIDC ID token. The
	// token is read from the `Authorization: Bearer <jwt>` gRPC
	// metadata header (or the equivalent HTTP header on the gateway
	// path). The request body never carries the token.
	AuthorizeWithOIDC(ctx context.Context, in *AuthorizeWithOIDCRequest, opts ...grpc.CallOption) (*AuthorizationResponse, error)
	// IssueChallenge mints a short-lived X.509 leaf cert bound to a
	// specific GitHub Actions run. The server validates the run exists
	// and is in progress by querying the GitHub API. The workflow is
	// expected to upload the returned cert as a GitHub Actions artifact
	// whose name starts with `chainguard-actions-client`, so a later
	// VerifyChallenge call can fetch and confirm origin. The server
	// keeps no state between Issue and Verify — the cert is
	// self-contained.
	IssueChallenge(ctx context.Context, in *IssueChallengeRequest, opts ...grpc.CallOption) (*IssueChallengeResponse, error)
	// VerifyChallenge lists the named run's artifacts via the GitHub
	// API, downloads any whose name starts with
	// `chainguard-actions-client`, and accepts the first whose cert is
	// signed by a currently-trusted KMS key, is unexpired, and is bound
	// to the requested (repository, run_id, run_attempt). The request
	// does NOT carry the cert — only run identity. Replay across runs
	// is prevented because artifacts are scoped to the run they were
	// uploaded from.
	VerifyChallenge(ctx context.Context, in *VerifyChallengeRequest, opts ...grpc.CallOption) (*AuthorizationResponse, error)
	// GetCACert returns the active signer's CA cert as PEM. External
	// verifiers consume this to validate attestation leaf certs
	// offline. For verification across a key-rotation overlap, prefer
	// GetTrustedKeys — GetCACert reflects only the *current* signing
	// key.
	GetCACert(ctx context.Context, in *GetCACertRequest, opts ...grpc.CallOption) (*CACertResponse, error)
	// GetTrustedKeys returns every public key the server currently
	// trusts for verification, as a list of PEM-encoded PKIX
	// `PUBLIC KEY` blocks. Includes the active signing key plus any
	// additional rotation-overlap keys configured at deploy time.
	GetTrustedKeys(ctx context.Context, in *GetTrustedKeysRequest, opts ...grpc.CallOption) (*TrustedKeysResponse, error)
}

type actionsAuthorizationClient struct {
	cc grpc.ClientConnInterface
}

func NewActionsAuthorizationClient(cc grpc.ClientConnInterface) ActionsAuthorizationClient {
	return &actionsAuthorizationClient{cc}
}

func (c *actionsAuthorizationClient) AuthorizeWithOIDC(ctx context.Context, in *AuthorizeWithOIDCRequest, opts ...grpc.CallOption) (*AuthorizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AuthorizationResponse)
	err := c.cc.Invoke(ctx, ActionsAuthorization_AuthorizeWithOIDC_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *actionsAuthorizationClient) IssueChallenge(ctx context.Context, in *IssueChallengeRequest, opts ...grpc.CallOption) (*IssueChallengeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IssueChallengeResponse)
	err := c.cc.Invoke(ctx, ActionsAuthorization_IssueChallenge_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *actionsAuthorizationClient) VerifyChallenge(ctx context.Context, in *VerifyChallengeRequest, opts ...grpc.CallOption) (*AuthorizationResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AuthorizationResponse)
	err := c.cc.Invoke(ctx, ActionsAuthorization_VerifyChallenge_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *actionsAuthorizationClient) GetCACert(ctx context.Context, in *GetCACertRequest, opts ...grpc.CallOption) (*CACertResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(CACertResponse)
	err := c.cc.Invoke(ctx, ActionsAuthorization_GetCACert_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *actionsAuthorizationClient) GetTrustedKeys(ctx context.Context, in *GetTrustedKeysRequest, opts ...grpc.CallOption) (*TrustedKeysResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(TrustedKeysResponse)
	err := c.cc.Invoke(ctx, ActionsAuthorization_GetTrustedKeys_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ActionsAuthorizationServer is the server API for ActionsAuthorization service.
// All implementations must embed UnimplementedActionsAuthorizationServer
// for forward compatibility.
//
// ActionsAuthorization issues authorization decisions for callers
// running inside GitHub Actions workflows.
//
// Two attestation mechanisms are provided:
//
//  1. AuthorizeWithOIDC verifies a GitHub Actions OIDC ID token
//     directly. This is the preferred path; trust derives from
//     GitHub's OIDC signing key.
//
//  2. IssueChallenge / VerifyChallenge implement an artifact-based
//     attestation, intended for workflows that *cannot* mint an OIDC
//     token at all. The most common case is a workflow run triggered
//     by a pull request from a fork: such runs do not get the
//     `id-token: write` permission elevated to a real token, so
//     AuthorizeWithOIDC is unavailable to them. The challenge flow
//     side-steps OIDC entirely and instead uses the run's own
//     artifact storage as the trust channel.
//
//     The server mints a short-lived X.509 leaf cert bound to the
//     run; the workflow uploads the cert as an artifact named with a
//     well-known prefix; verification fetches that artifact, validates
//     the cert against the trusted KMS public keys, and confirms its
//     URI SAN matches the requested run identity. Trust derives from
//     two facts: the cert is signed by our KMS key, and the
//     artifact's presence in the named run proves it was uploaded
//     from inside that run's runner (artifact upload requires
//     ACTIONS_RUNTIME_TOKEN, a per-runner credential GitHub never
//     exposes to workflow code). The mid-run logs API does not expose
//     new content until the run completes, so logs are not used.
//
// A successful AuthorizationResponse confirms the identity is real
// and that the caller is entitled to use this service. It is not a
// downstream permission grant — services that consume the returned
// `claims` (typically repository_owner_id and repository_visibility)
// apply their own authorization on top. Failure is signaled by a
// non-OK gRPC status (typically codes.PermissionDenied with no detail;
// see the README for the privacy rationale).
type ActionsAuthorizationServer interface {
	// AuthorizeWithOIDC verifies a GitHub Actions OIDC ID token. The
	// token is read from the `Authorization: Bearer <jwt>` gRPC
	// metadata header (or the equivalent HTTP header on the gateway
	// path). The request body never carries the token.
	AuthorizeWithOIDC(context.Context, *AuthorizeWithOIDCRequest) (*AuthorizationResponse, error)
	// IssueChallenge mints a short-lived X.509 leaf cert bound to a
	// specific GitHub Actions run. The server validates the run exists
	// and is in progress by querying the GitHub API. The workflow is
	// expected to upload the returned cert as a GitHub Actions artifact
	// whose name starts with `chainguard-actions-client`, so a later
	// VerifyChallenge call can fetch and confirm origin. The server
	// keeps no state between Issue and Verify — the cert is
	// self-contained.
	IssueChallenge(context.Context, *IssueChallengeRequest) (*IssueChallengeResponse, error)
	// VerifyChallenge lists the named run's artifacts via the GitHub
	// API, downloads any whose name starts with
	// `chainguard-actions-client`, and accepts the first whose cert is
	// signed by a currently-trusted KMS key, is unexpired, and is bound
	// to the requested (repository, run_id, run_attempt). The request
	// does NOT carry the cert — only run identity. Replay across runs
	// is prevented because artifacts are scoped to the run they were
	// uploaded from.
	VerifyChallenge(context.Context, *VerifyChallengeRequest) (*AuthorizationResponse, error)
	// GetCACert returns the active signer's CA cert as PEM. External
	// verifiers consume this to validate attestation leaf certs
	// offline. For verification across a key-rotation overlap, prefer
	// GetTrustedKeys — GetCACert reflects only the *current* signing
	// key.
	GetCACert(context.Context, *GetCACertRequest) (*CACertResponse, error)
	// GetTrustedKeys returns every public key the server currently
	// trusts for verification, as a list of PEM-encoded PKIX
	// `PUBLIC KEY` blocks. Includes the active signing key plus any
	// additional rotation-overlap keys configured at deploy time.
	GetTrustedKeys(context.Context, *GetTrustedKeysRequest) (*TrustedKeysResponse, error)
	mustEmbedUnimplementedActionsAuthorizationServer()
}

// UnimplementedActionsAuthorizationServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedActionsAuthorizationServer struct{}

func (UnimplementedActionsAuthorizationServer) AuthorizeWithOIDC(context.Context, *AuthorizeWithOIDCRequest) (*AuthorizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method AuthorizeWithOIDC not implemented")
}
func (UnimplementedActionsAuthorizationServer) IssueChallenge(context.Context, *IssueChallengeRequest) (*IssueChallengeResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method IssueChallenge not implemented")
}
func (UnimplementedActionsAuthorizationServer) VerifyChallenge(context.Context, *VerifyChallengeRequest) (*AuthorizationResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method VerifyChallenge not implemented")
}
func (UnimplementedActionsAuthorizationServer) GetCACert(context.Context, *GetCACertRequest) (*CACertResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetCACert not implemented")
}
func (UnimplementedActionsAuthorizationServer) GetTrustedKeys(context.Context, *GetTrustedKeysRequest) (*TrustedKeysResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method GetTrustedKeys not implemented")
}
func (UnimplementedActionsAuthorizationServer) mustEmbedUnimplementedActionsAuthorizationServer() {}
func (UnimplementedActionsAuthorizationServer) testEmbeddedByValue()                              {}

// UnsafeActionsAuthorizationServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ActionsAuthorizationServer will
// result in compilation errors.
type UnsafeActionsAuthorizationServer interface {
	mustEmbedUnimplementedActionsAuthorizationServer()
}

func RegisterActionsAuthorizationServer(s grpc.ServiceRegistrar, srv ActionsAuthorizationServer) {
	// If the following call panics, it indicates UnimplementedActionsAuthorizationServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ActionsAuthorization_ServiceDesc, srv)
}

func _ActionsAuthorization_AuthorizeWithOIDC_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(AuthorizeWithOIDCRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsAuthorizationServer).AuthorizeWithOIDC(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ActionsAuthorization_AuthorizeWithOIDC_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsAuthorizationServer).AuthorizeWithOIDC(ctx, req.(*AuthorizeWithOIDCRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ActionsAuthorization_IssueChallenge_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IssueChallengeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsAuthorizationServer).IssueChallenge(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ActionsAuthorization_IssueChallenge_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsAuthorizationServer).IssueChallenge(ctx, req.(*IssueChallengeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ActionsAuthorization_VerifyChallenge_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(VerifyChallengeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsAuthorizationServer).VerifyChallenge(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ActionsAuthorization_VerifyChallenge_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsAuthorizationServer).VerifyChallenge(ctx, req.(*VerifyChallengeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ActionsAuthorization_GetCACert_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetCACertRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsAuthorizationServer).GetCACert(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ActionsAuthorization_GetCACert_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsAuthorizationServer).GetCACert(ctx, req.(*GetCACertRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _ActionsAuthorization_GetTrustedKeys_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GetTrustedKeysRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsAuthorizationServer).GetTrustedKeys(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: ActionsAuthorization_GetTrustedKeys_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsAuthorizationServer).GetTrustedKeys(ctx, req.(*GetTrustedKeysRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// ActionsAuthorization_ServiceDesc is the grpc.ServiceDesc for ActionsAuthorization service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ActionsAuthorization_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "chainguard.platform.actions.ActionsAuthorization",
	HandlerType: (*ActionsAuthorizationServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "AuthorizeWithOIDC",
			Handler:    _ActionsAuthorization_AuthorizeWithOIDC_Handler,
		},
		{
			MethodName: "IssueChallenge",
			Handler:    _ActionsAuthorization_IssueChallenge_Handler,
		},
		{
			MethodName: "VerifyChallenge",
			Handler:    _ActionsAuthorization_VerifyChallenge_Handler,
		},
		{
			MethodName: "GetCACert",
			Handler:    _ActionsAuthorization_GetCACert_Handler,
		},
		{
			MethodName: "GetTrustedKeys",
			Handler:    _ActionsAuthorization_GetTrustedKeys_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "actions.platform.proto",
}

const (
	Actions_Record_FullMethodName = "/chainguard.platform.actions.Actions/Record"
)

// ActionsClient is the client API for Actions service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Actions is the operational API for callers running inside GitHub
// Actions workflows. Unlike ActionsAuthorization (which only issues
// trust decisions), this service hosts RPCs that workflows invoke
// against the service during a run — self-reported telemetry today,
// and likely more workflow-facing operations later.
//
// Authentication is OPTIONAL. Callers MAY present a GitHub Actions
// OIDC ID token in the `Authorization: Bearer <jwt>` header (same
// audience as ActionsAuthorization.AuthorizeWithOIDC). When a valid
// token is presented, the recorded event is marked verified and the
// token's claims (repository, run_id, run_attempt, sub, ...) are
// attached. When no token is presented, the event is still recorded
// — just marked unverified. When a token IS presented but fails to
// verify, the call is rejected.
type ActionsClient interface {
	// Record registers that a GitHub repository used this service.
	// It is write-only and the recording is internal: the response is
	// empty and there is no read side.
	//
	// Behavior depends on the `Authorization: Bearer <jwt>` header:
	//   - Header absent (or non-Bearer scheme): the request body's
	//     `repository` field is REQUIRED and taken as-is; the event is
	//     recorded as unverified. Lets forked-PR workflows (which
	//     cannot mint an OIDC token) still self-identify.
	//   - Header present and verifies: the request body's `repository`
	//     field is IGNORED and the repository is taken from the
	//     verified token's claim instead; the event is recorded as
	//     verified with the token's claims attached.
	//   - Header present but fails to verify: rejected with
	//     codes.PermissionDenied. The caller claimed an identity and
	//     we won't silently downgrade that to unverified.
	//
	// Repeated calls with the same token are allowed (no replay check)
	// so a long-running workflow can record multiple events.
	Record(ctx context.Context, in *RecordRequest, opts ...grpc.CallOption) (*RecordResponse, error)
}

type actionsClient struct {
	cc grpc.ClientConnInterface
}

func NewActionsClient(cc grpc.ClientConnInterface) ActionsClient {
	return &actionsClient{cc}
}

func (c *actionsClient) Record(ctx context.Context, in *RecordRequest, opts ...grpc.CallOption) (*RecordResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RecordResponse)
	err := c.cc.Invoke(ctx, Actions_Record_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// ActionsServer is the server API for Actions service.
// All implementations must embed UnimplementedActionsServer
// for forward compatibility.
//
// Actions is the operational API for callers running inside GitHub
// Actions workflows. Unlike ActionsAuthorization (which only issues
// trust decisions), this service hosts RPCs that workflows invoke
// against the service during a run — self-reported telemetry today,
// and likely more workflow-facing operations later.
//
// Authentication is OPTIONAL. Callers MAY present a GitHub Actions
// OIDC ID token in the `Authorization: Bearer <jwt>` header (same
// audience as ActionsAuthorization.AuthorizeWithOIDC). When a valid
// token is presented, the recorded event is marked verified and the
// token's claims (repository, run_id, run_attempt, sub, ...) are
// attached. When no token is presented, the event is still recorded
// — just marked unverified. When a token IS presented but fails to
// verify, the call is rejected.
type ActionsServer interface {
	// Record registers that a GitHub repository used this service.
	// It is write-only and the recording is internal: the response is
	// empty and there is no read side.
	//
	// Behavior depends on the `Authorization: Bearer <jwt>` header:
	//   - Header absent (or non-Bearer scheme): the request body's
	//     `repository` field is REQUIRED and taken as-is; the event is
	//     recorded as unverified. Lets forked-PR workflows (which
	//     cannot mint an OIDC token) still self-identify.
	//   - Header present and verifies: the request body's `repository`
	//     field is IGNORED and the repository is taken from the
	//     verified token's claim instead; the event is recorded as
	//     verified with the token's claims attached.
	//   - Header present but fails to verify: rejected with
	//     codes.PermissionDenied. The caller claimed an identity and
	//     we won't silently downgrade that to unverified.
	//
	// Repeated calls with the same token are allowed (no replay check)
	// so a long-running workflow can record multiple events.
	Record(context.Context, *RecordRequest) (*RecordResponse, error)
	mustEmbedUnimplementedActionsServer()
}

// UnimplementedActionsServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedActionsServer struct{}

func (UnimplementedActionsServer) Record(context.Context, *RecordRequest) (*RecordResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Record not implemented")
}
func (UnimplementedActionsServer) mustEmbedUnimplementedActionsServer() {}
func (UnimplementedActionsServer) testEmbeddedByValue()                 {}

// UnsafeActionsServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ActionsServer will
// result in compilation errors.
type UnsafeActionsServer interface {
	mustEmbedUnimplementedActionsServer()
}

func RegisterActionsServer(s grpc.ServiceRegistrar, srv ActionsServer) {
	// If the following call panics, it indicates UnimplementedActionsServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Actions_ServiceDesc, srv)
}

func _Actions_Record_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RecordRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(ActionsServer).Record(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Actions_Record_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(ActionsServer).Record(ctx, req.(*RecordRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// Actions_ServiceDesc is the grpc.ServiceDesc for Actions service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Actions_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "chainguard.platform.actions.Actions",
	HandlerType: (*ActionsServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Record",
			Handler:    _Actions_Record_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "actions.platform.proto",
}
